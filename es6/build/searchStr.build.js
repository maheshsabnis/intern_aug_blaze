"use strict";

var data = 'Object is similar to Map—both for settings, let you set keys to values, retrieve those values, delete keys, and detect whether something is stored at a key. For this reason (and because there were no built-in alternatives), Object has been used as Map historically.'; // Symbol.search()
// accept a data in its constructor, based on the data, the expression is created by symbol
// when search operation is performed, the expression will be compared based on the 'unique-natch' of the expression  

console.log(data.search('set'));
