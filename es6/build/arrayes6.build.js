"use strict";

var names = ['Mahesh', 'Ajay', 'Akash', 'Raju', 'Amit', 'Mahesh', 'Akash', 'Pushkar']; // Using the for..of iterator to read or iterate over the array

for (var _i = 0, _names = names; _i < _names.length; _i++) {
  var n = _names[_i];
  console.log("Name = ".concat(n));
}
